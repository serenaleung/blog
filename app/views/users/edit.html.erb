<div class="container">
  <h1>Change Password</h1>
  <%= @user.errors.full_messages.join(', ') %>

  <%# if you pass a pesisted ActiveRecord object to the `form_for` (in the DB)
      then `form_for` will submit a `PATCH` request (instead of POST) using a
      hidden_field technique and the URL for the form will be in the case the
      `update` action url which is in our case user_path(@user) %>

  <%= form_for @user do |f| %>
    <div>
      <%= f.label :current_password %>
      <%= f.text_field :current_password, class: "form-control" %>
    </div>
    <br>
    <div>
      <%= f.label :new_password %>
      <%= f.text_area :new_password, class: "form-control commentHeight" %>
    </div>
    <div>
      <%= f.label :new_password_confirmation %>
      <%= f.text_area :new_password_confirmation, class: "form-control commentHeight" %>
    </div>
    <br>
    <%= f.submit "Submit", class: "btn btn-default" %>
  <% end %>
</div>
